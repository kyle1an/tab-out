# Store Saved Pages Locally Instead of Chrome Bookmarks

Saved Pages are Tab Out-owned dashboard state, not Chrome bookmarks. They are stored in extension-local state so they can carry Tab Out-specific metadata, merge with open tabs in the Tabs source, and avoid creating visible bookmark-folder side effects or conflicts with the existing Bookmarks source.
